import ServiceContractShell from './ServiceContractShell'

export default function ServiceContractLayout({ children }: { children: React.ReactNode }) {
  return <ServiceContractShell>{children}</ServiceContractShell>
}
