.git
.next
node_modules
screenshots
.claude
.agents
.deepseek
*.pdf
*.docx
*.xlsx
*.mov
*.mp4
*.png
!public/**/*.png
!public/**/*.jpg
!public/**/*.jpeg
!public/**/*.webp

# data/ carries runtime seed assets (the project-schedule workbook). Without this
# the *.xlsx rule above silently strips them and the pages that read them 500.
!data/**
